# Advanced-Python
hello there
This file contains the codes which we have used in our internship
This repository file contains the code and  practice qs which we used for our learning process and it helps us to get a grasp of the basics of python also
commands used in git for cloud
[git init=intialize it locally
git clone=clone the original repo or simply duplicating
git pull=add up the latest update.for ex-if ither co workers had worked on this repo if we work without updating then it will be a problem so this command will pull the other updates which have updated  newly
git status= the stage at which the file has bbeen currently is there any file to be updated like that
git add= to give an html files to add into the cloud like these much files are ready and they are ready to save 
git commit= we can save the change which is actually ready. for ex-commit the chnge or save the change
git push= pushes the saved content to the cloud of git hub
merge= if branches are completed this can be updated using merge command]
